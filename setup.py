from setuptools import setup, find_packages

setup(
    name='wxauto',
    version='3.9.11.17',  # 可以自定义版本号
    description='A fork of wxauto for WeChat automation',
    packages=find_packages(), # 这会自动找到wxauto目录
)